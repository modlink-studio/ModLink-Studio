# Plugin Scaffold CLI

The interactive scaffold tool now lives in
`apps/modlink_plugin_scaffold/`.

Run it from this repository with:

```bash
uv run modlink-plugin-scaffold
```

Generated driver projects are installed with standard `pip` commands, for
example:

```bash
python -m pip install -e ./my_driver
python -m modlink_studio
```
