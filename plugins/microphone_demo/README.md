# Microphone Demo Plugin

Minimal example driver that turns the local microphone into a ModLink plugin.

Recommended startup from the repository root:

```powershell
uv sync
uv run --with .\plugins\microphone_demo modlink-studio
```
